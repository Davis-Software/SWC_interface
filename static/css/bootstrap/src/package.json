{
    "name": "swc_interface-kelpframework",
    "version": "0.5.6",
    "description": "",
    "scripts": {
        "build": "sass --source-map ./scss:./.. --style compressed",
        "watch": "nodemon -e scss -x 'npm run build'",
        "fast-watch": "nodemon -e scss -x 'npm run fast-build'",
        "fast-build": "sass ./scss:./.."
    },
    "author": "Davis_Software",
    "devDependencies": {
        "sass": "^1.26.7"
    },
    "dependencies": {
        "nodemon": "^2.0.12"
    }
}
